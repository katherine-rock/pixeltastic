<header id="top">
    <div class="header_image">
        <%= image_tag "neon_camera_30cm.png" %>
    </div>

    <div class="header_text">
        <h1>Pixeltastic</h1>
    </div>
</header>

<br>

<section class="log_status">
    <% if user_signed_in? %>
        <p>Currently signed in as:&nbsp;</p><%= current_user.email %>
    <% else %>
        <p>You are currently visiting as a guest.</p>
    <% end %>
</section>